# Docker uses the journald log driver on Server 5. Keep container output in
# journald (and available through `docker logs`) instead of duplicating every
# line into /var/log/messages through imjournal.
if $inputname == "imjournal" and re_match($programname, "^[0-9a-f]{12}\$") then stop
